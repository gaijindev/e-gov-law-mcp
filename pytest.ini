[pytest]
testpaths = tests
asyncio_mode = auto
# Live tests hit the real e-Gov API; opt in with `pytest -m live`.
addopts = -m "not live"
markers =
    live: exercises the real e-Gov 法令API over the network
